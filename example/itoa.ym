def main() I32
  assert(0.to_s == "0")
  assert(110.to_s == "110")
  assert(786235.to_s == "786235")
  assert((-1).to_s == "-1") # TODO: - should bind stronger to the number
  assert(U32(0).to_s == "0")
  assert(U32(15870).to_s == "15870")

  return 0
end
