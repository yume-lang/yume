fn hello()
  let a = U8[?h, ?e, ?l, ?l, ?o]
  printf("string: %s\n", a.c_str)
  printf("size of a: %d\n", a.size)
  printf("size of a.c_str: %d\n", a.c_str.size)
  printf("ptr of a: %p\n", a.ptr)
  return
end

fn main() I32
  hello()
  return 0
end
