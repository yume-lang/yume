def hello()
  let a = U8:[?h, ?e, ?l, ?l, ?o]
  printf("string: %s\n".c_str.unsafe_ptr, a.c_str)
  printf("size of a: %d\n".c_str.unsafe_ptr, a.size)
  printf("size of a.c_str: %d\n".c_str.unsafe_ptr, a.c_str.size)
  printf("ptr of a: %p\n".c_str.unsafe_ptr, a.unsafe_ptr)
  return
end

def main() I32
  hello()
  return 0
end
