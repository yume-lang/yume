__primitive__(slice_size) size<T>(arr T[]) I32

struct Test
  body U8[]
  size I32
end

fn capacity(d Test) I32 = d::body.size
fn size(d Test) I32 = d::size

fn foo(a I32, b I32, c I32) I32 = a

fn foo(a I32, b I32) I32 = b

fn foo(a U8, b I32) U8 = a

fn foo(a I32, b U8) U8 = b

fn main() I32
  foo(1, 2, 3)
  foo(4, 5)
  foo(?a, 4)
  foo(5, ?b)
  let d Test = Test(U8[](0), 0)
  return d.capacity
end
